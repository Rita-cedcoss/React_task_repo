import logo from './logo.svg';
import './App.css';

function App() {
  return (
   <>
    <p>
    Create a react app with following features
Show total corona virus cases, total deaths and Total recoveries 
Display country wise data in tabular format where we can sort whole table according to each column 
Use object key as column name and value as column data
create a single click for sorting ascending or descending (Toggle)  
Sample data is attached
    </p>
   </>
  );
}

export default App;
